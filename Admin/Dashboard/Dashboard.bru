meta {
  name: Dashboard
  type: http
  seq: 1
}

get {
  url: {{baseIP_url}}/v1/admin/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
