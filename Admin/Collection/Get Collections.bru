meta {
  name: Get Collections
  type: http
  seq: 1
}

get {
  url: {{live_url}}/v1/collections
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
