meta {
  name: Delete Collection
  type: http
  seq: 5
}

delete {
  url: {{baseIP_url}}/v1/collections/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
