meta {
  name: Get All Users
  type: http
  seq: 1
}

get {
  url: {{baseIP_url}}/v1/admin/users?filter[search]=John
  body: none
  auth: bearer
}

params:query {
  filter[search]: John
  ~filter[status]: active
}

auth:bearer {
  token: {{adminToken}}
}
